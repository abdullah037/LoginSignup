# Simple Full-Stack App

A streamlined project featuring a Node.js backend and a static frontend.

## 🛠️ Setup & Installation

### 1. Backend Setup
1. Navigate to the folder: `cd backend`
2. Install dependencies: `npm install`
3. Configure `.env`: Ensure you have your `PORT` and DB strings set.
4. Start: `npm start`

### 2. Frontend Setup
1. Navigate to the folder: `cd frontend`
2. Configure `.env`: Add your `API_URL` (usually `http://localhost:PORT`).
3. Start npx serve

---

## 🔒 Security Note
This project is configured for **DEVOPS Deployment Tasks**. 